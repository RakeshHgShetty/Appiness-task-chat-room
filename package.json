{
  "name": "real_time_chat_room",
  "version": "1.0.0",
  "description": "Registered user can login to the chat room and they can chat with the registered user.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Rakesh HG",
  "license": "ISC",
  "dependencies": {
    "bodyParser": "*",
    "express": "*",
    "mongodb": "^2.2.5",
    "socket.io": "*"
  }
}
